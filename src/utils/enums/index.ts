export enum TypeEnums {
  JSON = `json`,
  NUMBER = `number`,
  STRING = `string`,
  OBJECT = `object`,
  BOOLEAN = `boolean`,
}

export enum RequestMethodEnum {
  GET = `GET`,
  POST = `POST`,
  PUT = `PUT`,
  DELETE = `DELETE`,
}
